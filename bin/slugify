#!/bin/sh

# Converts text to URL-friendly slug format.
#
# Usage:
#   slugify "Hello World!"
#   slugify "http://example.com"

echo $1 | \
  iconv -t ascii//TRANSLIT | \
  tr A-Z a-z | \
  sed -E 's/[^a-z0-9]+//g' | \
  sed -E 's/(www|https|http)+//g'
