#!/bin/sh

if [ -z "$1" ]; then
  echo "Usage: git cog <search-term>"
  exit 1
fi

search_term="$1"

branch=$(git branch | ag "$search_term" | head -n 1 | sed 's/^\* //')

if [ -z "$branch" ]; then
  echo "No branch found matching '$search_term'"
  exit 1
fi

git checkout "$branch"
